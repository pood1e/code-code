apiVersion: v1
kind: Service
metadata:
  name: platform-chat-service
spec:
  selector:
    app.kubernetes.io/name: platform-chat-service
  ports:
    - name: grpc
      port: 8081
      targetPort: grpc
