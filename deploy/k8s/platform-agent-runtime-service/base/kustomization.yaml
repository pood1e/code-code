apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: code-code

resources:
  - crds.yaml
  - serviceaccount.yaml
  - role.yaml
  - rolebinding.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: code-code/platform-agent-runtime-service
    newTag: 0.0.0
