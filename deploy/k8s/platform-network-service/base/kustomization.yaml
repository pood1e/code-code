apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: code-code

resources:
  - serviceaccount.yaml
  - role.yaml
  - rolebinding.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: code-code/platform-network-service
    newTag: 0.0.0
