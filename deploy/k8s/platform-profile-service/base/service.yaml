apiVersion: v1
kind: Service
metadata:
  name: platform-profile-service
spec:
  selector:
    app.kubernetes.io/name: platform-profile-service
  ports:
    - name: grpc
      port: 8081
      targetPort: grpc
