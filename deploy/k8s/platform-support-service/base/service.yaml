apiVersion: v1
kind: Service
metadata:
  name: platform-support-service
spec:
  selector:
    app.kubernetes.io/name: platform-support-service
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: grpc
      port: 8081
      targetPort: grpc
