apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: code-code-observability

resources:
  - rbac.yaml
  - configmap.yaml
  - rules-configmap.yaml
  - service.yaml
  - statefulset.yaml
